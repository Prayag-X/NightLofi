import 'package:flutter/material.dart';

class ProfileOtherPage extends StatelessWidget {
  const ProfileOtherPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}