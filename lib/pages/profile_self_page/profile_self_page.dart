import 'package:flutter/material.dart';

class ProfileSelfPage extends StatelessWidget {
  const ProfileSelfPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}